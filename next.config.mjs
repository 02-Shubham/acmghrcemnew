/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'aceternity.com','me7aitdbxq.ufs.sh'], // Add aceternity.com to the list
    },
};

export default nextConfig; // Use ES module export syntax
